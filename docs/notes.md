# Notes

- Cloudflare R2 needs a custom domain. The one provided by Cloudflare is heaviliy rate-limited
- The cache URL set in `extra-substituters` is Cloudflare R2 custom domain, _not_ the admin interface on `.fly.dev`
